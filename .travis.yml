sudo: required
services:
  - docker
language: python
python:
  - 3.6
jobs:
  include:
    - stage: "build"
      script:
        - make start
    - stage: "test"
      script:
        - echo Test1
        - echo Test2
    - stage: "deploy"
      script:
        - echo deployment
after_success:
  - echo "Success"