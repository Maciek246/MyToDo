sudo: required
services:
  - docker
language: python
python:
  - 3.6
jobs:
  include:
    - stage: "build"
      script:
        - pip install -r requirements.txt
        - npm install
    - stage: "test"
      script:
        - manage.py test
    - stage: "deploy"
      script:
        - echo deployment
after_success:
  - echo "Success"